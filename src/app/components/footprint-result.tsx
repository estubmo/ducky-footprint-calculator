"use client";

export function FootprintResult({ name }: { name: string }) {
    return (
        <div className="flex flex-col items-center justify-center">
            Hello, {name}
        </div>
    );
}
